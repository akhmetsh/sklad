# PostgreSQL connection string
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/warehouse_db?schema=public"

# Auth.js secret - generate with: openssl rand -base64 32
AUTH_SECRET="your-auth-secret-here"

# NEXTAUTH_URL — only needed in production. In dev, Auth.js v5 auto-detects
# from request headers, so this can be left commented out.
# NEXTAUTH_URL="https://sklad.yourcompany.kz"

# Skip the ERD generator in production. The rendered docs/ERD.svg is already
# committed, so production builds don't need to re-render it (avoids pulling
# in Chromium via @mermaid-js/mermaid-cli).
# Set this to "true" in Render/Vercel/etc. Leave unset locally for `npm run db:erd`.
# DISABLE_ERD="true"
